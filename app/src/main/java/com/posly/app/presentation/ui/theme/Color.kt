package com.posly.app.presentation.ui.theme

import androidx.compose.ui.graphics.Color

// ─── PosLy Design System — Brand Color Tokens ─────────────────────────────

/** Pure White — latar utama layar kasir, tablet split-pane, dan form setting */
val Background = Color(0xFFFFFFFF)

/** Soft Cloud Grey — kartu produk di katalog, card ringkasan transaksi, border pemisah */
val SurfaceCard = Color(0xFFF8FAFC)

/** Soft Slate Blue — tombol Proses Bayar, tab navigasi aktif, ikon utama aplikasi */
val Primary = Color(0xFF4F6BED)

/** Soft Slate Blue — darker variant for pressed states */
val PrimaryDark = Color(0xFF3A54D4)

/** Soft Blue Wash — highlight item terpilih di keranjang, pill filter kategori aktif */
val PrimaryContainer = Color(0xFFEEF2FF)

/** On Primary — white text on primary buttons */
val OnPrimary = Color(0xFFFFFFFF)

/** On Primary Container — primary text on primary container */
val OnPrimaryContainer = Color(0xFF4F6BED)

/** Deep Slate — judul menu, total nominal harga, teks invoice */
val TextPrimary = Color(0xFF1E293B)

/** Muted Steel Grey — catatan order, SKU produk, tanggal & jam transaksi */
val TextSecondary = Color(0xFF64748B)

/** Soft Outline — garis pemisah tabel, border kartu produk, outline tombol secondary */
val BorderDivider = Color(0xFFE2E8F0)

// ─── Semantic / Status Colors ──────────────────────────────────────────────

/** Success Green — stok normal, transaksi berhasil */
val SuccessGreen = Color(0xFF10B981)
val SuccessGreenLight = Color(0xFFD1FAE5)

/** Warning Amber — stok menipis, peringatan */
val WarningAmber = Color(0xFFF59E0B)
val WarningAmberLight = Color(0xFFFEF3C7)

/** Error Red — stok habis, void transaksi, error */
val ErrorRed = Color(0xFFEF4444)
val ErrorRedLight = Color(0xFFFEE2E2)

/** Info Blue — informasi, QRIS, notifikasi */
val InfoBlue = Color(0xFF3B82F6)
val InfoBlueLight = Color(0xFFEFF6FF)

// ─── Financial Palette ─────────────────────────────────────────────────────

/** Omzet / Revenue indicator */
val OmzetBlue = Color(0xFF4F6BED)
val OmzetBlueBg = Color(0xFFEEF2FF)

/** HPP / Cost indicator */
val HppOrange = Color(0xFFF97316)
val HppOrangeBg = Color(0xFFFFF7ED)

/** Laba Kotor / Gross Profit */
val LabaHijau = Color(0xFF10B981)
val LabaHijauBg = Color(0xFFECFDF5)

/** Laba Bersih / Net Profit — premium gold */
val LabaBersihGold = Color(0xFFD97706)
val LabaBersihGoldBg = Color(0xFFFEF9C3)

// ─── Budget Allocation Pos Colors ─────────────────────────────────────────

/** Pos Bahan Baku */
val PosBahanBaku = Color(0xFF8B5CF6)
val PosBahanBakuBg = Color(0xFFF5F3FF)

/** Pos Tabungan Bisnis */
val PosTabungan = Color(0xFF06B6D4)
val PosTabunganBg = Color(0xFFECFEFF)

/** Pos Dana Darurat */
val PosDarurat = Color(0xFFEF4444)
val PosDaruratBg = Color(0xFFFEE2E2)

/** Pos Transport */
val PosTransport = Color(0xFF10B981)
val PosTransportBg = Color(0xFFECFDF5)

// ─── Neutral Scale ─────────────────────────────────────────────────────────
val Neutral50 = Color(0xFFF8FAFC)
val Neutral100 = Color(0xFFF1F5F9)
val Neutral200 = Color(0xFFE2E8F0)
val Neutral300 = Color(0xFFCBD5E1)
val Neutral400 = Color(0xFF94A3B8)
val Neutral500 = Color(0xFF64748B)
val Neutral600 = Color(0xFF475569)
val Neutral700 = Color(0xFF334155)
val Neutral800 = Color(0xFF1E293B)
val Neutral900 = Color(0xFF0F172A)

// ─── Scrim & Overlay ───────────────────────────────────────────────────────
val Scrim = Color(0x801E293B)
val OverlayLight = Color(0x0A000000)
